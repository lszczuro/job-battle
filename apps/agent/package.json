{
  "name": "@repo/agent",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uv run langgraph dev --port 8123 --no-browser",
    "test": "uv run --group dev pytest tests/",
    "postinstall": "uv sync --group dev"
  },
  "devDependencies": {
    "@langchain/langgraph-cli": "^1.1.12"
  }
}